#include "emerald.h"

void parse_buffer (char* code, int size) {
/*
  queue* tokens = q_create();
  int position = 0;
  int line = 1;
  const char newline[] = "\n";

  while (1) {
    char ch = code[position];
    printf("");
  }

///
  for (int i = 0; i < size; i++) {
    token tok;
    tok.type = 0;
    tok.value = buf[i];
    q_enqueue(tokens, tok);
  }
//

  q_dump(tokens);
*/
}
